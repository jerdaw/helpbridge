# Enable strict engine checking (fail if Node version doesn't match package.json)
engine-strict=true

# Use exact versions for reproducible builds
save-exact=true

# Prevent npm from automatically creating package-lock.json on every install
# (we already have one, no need to update unless explicitly running npm install)
package-lock=true
